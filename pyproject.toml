[project]
name = "sentencebank"
version = "0.1.0"
description = "A sentence bank."
authors = [
    {name = "Your Name",email = "you@example.com"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.group.dev.dependencies]
pytest = "9.0.2"
coverage = "7.13.0"

[tool.coverage.run]
omit = ["tests/**/*.py"]
